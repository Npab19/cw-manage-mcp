<section class="card error">
  <h1>404 — wizard not available</h1>
  <p>Either the bootstrap code is missing/expired or setup is already complete.</p>
  <p>If this is a fresh deploy and setup hasn't completed yet, check the container logs for the <code>[BOOTSTRAP]</code> line — that URL rotates on every container restart while setup is pending.</p>
</section>
